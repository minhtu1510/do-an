# ICSScout Requirements
# Industrial Control Systems Reconnaissance Framework

# Core dependencies
python-snap7>=1.3          # S7 protocol support (Siemens PLCs)
pymodbus>=3.5.0           # Modbus TCP/RTU protocol
opcua>=0.98.13            # OPC UA protocol (asyncua fork recommended for Python 3.11+)
asyncua>=1.0.0            # Async OPC UA client

# Network scanning and packet capture
scapy>=2.5.0              # Packet manipulation and capture
netifaces>=0.11.0         # Network interface enumeration (cross-platform)
python-nmap>=0.7.1        # Nmap integration

# Web framework and real-time communication
flask>=3.0.0              # Web framework
flask-socketio>=5.3.0     # WebSocket support for real-time dashboard
flask-cors>=4.0.0         # CORS support
python-socketio>=5.10.0   # SocketIO client/server
eventlet>=0.33.0          # Async networking library for SocketIO

# CLI interface
prompt_toolkit>=3.0.43    # Interactive CLI
rich>=13.7.0              # Beautiful terminal output
click>=8.1.7              # CLI argument parsing

# Data processing and analysis
pandas>=2.1.0             # Data analysis
numpy>=1.26.0             # Numerical computing

# Database
sqlalchemy>=2.0.0         # ORM for session/CVE database
alembic>=1.13.0           # Database migrations

# Security and cryptography
cryptography>=41.0.0      # Crypto utilities (for S7-PLUS)
bcrypt>=4.1.0             # Password hashing
pyjwt>=2.8.0              # JWT tokens for authentication

# Reporting
jinja2>=3.1.2             # Template engine
markdown>=3.5.0           # Markdown support in reports
pyyaml>=6.0.1             # YAML configuration files
python-docx>=1.1.0        # Word document generation
reportlab>=4.0.0          # PDF generation

# Utilities
python-dateutil>=2.8.2    # Date/time utilities
requests>=2.31.0          # HTTP requests
urllib3>=2.1.0            # HTTP library
colorama>=0.4.6           # Colored terminal output (Windows compatibility)
tqdm>=4.66.0              # Progress bars

# Development dependencies (optional)
pytest>=7.4.0             # Testing framework
pytest-cov>=4.1.0         # Coverage plugin
black>=23.12.0            # Code formatter
flake8>=6.1.0             # Linter
mypy>=1.7.0               # Type checker

# Platform-specific dependencies
# Windows only:
# pywin32>=306             # Windows API (for WMI, etc.) - install manually on Windows
# wmi>=1.5.1               # Windows Management Instrumentation

# Linux only (install via system package manager):
# python3-dev              # Python development headers
# libpcap-dev              # Packet capture library

# Note: For packet capture to work, you need:
# - Linux: Run as root or configure capabilities: sudo setcap cap_net_raw,cap_net_admin+eip $(which python3)
# - Windows: Run as Administrator and install Npcap (https://npcap.com/)
